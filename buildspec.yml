version: 0.2

phases:
  pre_build:
    commands:
      - echo 'hello'
  build:
    commands:
      - 'npm test'
  post_build:
    commands:
      - 'forever stopall'
artifacts:
  files:
    - 'test/*'
    - 'app.js'
    - 'package.json'
    - 'buildspec.yml'
    - 'appspec.yml'
